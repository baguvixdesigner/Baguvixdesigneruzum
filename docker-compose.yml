services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: uzum_bot
      POSTGRES_PASSWORD: uzum_bot
      POSTGRES_DB: uzum_bot
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
